package com.example.app_citas.login.domain

import com.example.app_citas.login.data.model.LoginRequest

class LoginUseCase {
//    private val repository = LoginRepository()
//
//    suspend operator fun invoke(user: LoginRequest): Result<APIResponse<UserDTO>> {
//        val result = repository.login(user)
//        return result
//    }
}